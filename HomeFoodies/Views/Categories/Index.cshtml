@model IEnumerable<HomeFoodies.Models.Category>

@{
    Layout = "~/Views/Admin/Shared/_LayoutAdmin.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ItemCategoryDesc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsActive)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ItemCategoryDesc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsActive)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ItemCategoryID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ItemCategoryID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ItemCategoryID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
