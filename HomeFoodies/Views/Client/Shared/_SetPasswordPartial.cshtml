@using HomeFoodies.Models
@model Supplier
@{
    ViewBag.Title = "Set Password";
}

<div class="modal-dialog modal-login-resized">
    <div class="modal-content">
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
            <h4 class="modal-title">Set Password</h4>
        </div>
        <div class="modal-body">
            <div class="scroller" style="height:250px" data-always-visible="1" data-rail-visible1="1">
                @using (Html.BeginForm("SetPassword", "Suppliers", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "login-form", role = "form" }))
                {
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true, "", new { @class = (ViewBag.ValidLogin == null || ViewBag.ValidLogin) ? "alert alert-danger display-hide" : "alert alert-danger" })
                    <div class="portlet-body form">
                        <div class="row">
                            <div class="form-body">
                                <div class="col-md-1">
                                </div>
                                <div class="col-md-10">
                                    <div class="form-group form-md-line-input has-warning">
                                        <div class="input-group">
                                            <span class="input-group-addon">
                                                <i class="icon-envelope"></i>
                                            </span>
                                            @Html.HiddenFor(m => m.FullName, new { @class = "form-control", @placeholder = "", @autocomplete = "off" })
                                            @Html.HiddenFor(m => m.LoginUser.UserEmail, new { @class = "form-control", @placeholder = "", @autocomplete = "off" })
                                            @Html.HiddenFor(m => m.LoginUser.UserID, new { @class = "form-control", @placeholder = "", @autocomplete = "off" })
                                            @Html.PasswordFor(m => m.LoginUser.UserPassword, new { @class = "form-control", @placeholder = "", @autocomplete = "off" })
                                            @Html.ValidationMessageFor(m => m.LoginUser.UserPassword, "", new { @class = "text-danger" })
                                            <label for="form_control_1 font-input" class="font-input">Enter Password</label>
                                            <span class="help-block"></span>
                                        </div>
                                    </div>
                                    <div class="form-group form-md-line-input has-warning">
                                        <div class="input-group">
                                            <span class="input-group-addon">
                                                <i class="icon-key"></i>
                                            </span>
                                            @Html.PasswordFor(m => m.LoginUser.UserPassword, new { @class = "form-control", @placeholder = "Password", @autocomplete = "off" })
                                            @Html.ValidationMessageFor(m => m.LoginUser.UserPassword, "", new { @class = "text-danger" })
                                            <label for="form_control_1 font-input" class="font-input">Confirm Password</label>
                                            <span class="help-block"></span>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-1">
                                </div>
                            </div>
                        </div>

                        <div class="modal-footer">
                            <button type="button" data-dismiss="modal" class="btn default">Close</button>
                            <input type="submit" value="Submit" class="btn btn-warning" />
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>
